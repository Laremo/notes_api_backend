const notesController = {};

notesController.SaveNote = (req, res) => {
  //use service with try
  try {
    console.log(`L+ogica`);
  } catch (Exception) {
    console.log();
  }
};

modules.export = notesController;
